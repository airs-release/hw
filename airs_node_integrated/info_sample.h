#ifndef INFO_H
#define INFO_H
// USE_HARDCODED_WIFI 를 켠 경우에만 필요.
// 이 파일을 같은 폴더에 info.h 로 복사한 뒤 값을 채운다.
// ⚠️ 2.4GHz Wi-Fi만 지원. info.h(실제 값)는 비밀이니 git/슬랙/문서에 올리지 말 것.
const char* ssid     = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";
#endif
